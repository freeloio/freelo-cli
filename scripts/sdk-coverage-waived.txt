# SDK operations intentionally NOT wrapped by a typed CLI command.
#
# Format: one SDK method name per line, optional trailing comment. Blank
# lines and #-comments ignored. This is the ONLY place non-coverage is
# declared — sdk-gap.sh treats every name here as "covered" so the gap
# check stays green. Keep a reason on each line; review on every SDK bump.
#
# Categories below are documentation only (all are treated identically).

# --- Not supported server-side (return 404; see CLAUDE.md gotchas) ---
GetPublicLinkToTask        # /task/{id}/public-link 404s (non-paid / nonexistent)
DeletePublicLinkToTask     # ditto
SetUserTimeEstimate        # /task/{id}/user-time-estimate 404
DeleteUserTimeEstimate     # ditto
EditTasklist               # POST /tasklist/{id} (edit) 404

# --- Covered via alternate endpoint or `freelo api` passthrough ---
GetAllProjects             # `projects list` uses GetProjects; alt listing via `freelo api get /all-projects`
GetUserProjects            # alt project listing; use `freelo api get` if needed
GetInvitedProjects         # `freelo api get /invited-projects`
GetArchivedProjects        # `freelo api get /archived-projects`

# --- Backlog: genuine endpoints not yet given a command (TODO) ---
# Each is a candidate for a real command. Move it out of this file when wrapped.
EditTimeTracking           # TODO: tracking edit — `freelo tracking edit`
CreateTaskReminder         # TODO: new reminders capability
DeleteTaskReminder         # TODO: new reminders capability
GetTaskRelations           # TODO: task relations ("vazby mezi úkoly")
FindTaskRelationsBulk      # TODO: task relations bulk
SetTotalTimeEstimate       # TODO: total time estimate (distinct from user estimate; probe live)
DeleteTotalTimeEstimate    # TODO: total time estimate
AssignTaskToProject        # TODO: task<->project assignment
RemoveTaskFromProject      # TODO: task<->project assignment
DeleteTask                 # TODO: destructive; decide if `tasks delete` is desired
GetFinishedTasks           # TODO: `tasks list --finished`
GetAllStates               # TODO: states listing
GetAssignableWorkers       # TODO: `workers assignable`
FindAvailableTaskLabels    # TODO: task-label listing (have project-label listing)
RemoveProjectWorkersByIds  # TODO: by-IDs variant (have by-emails)
GetProjectManagerOf        # TODO: project manager listing
DownloadIssuedInvoiceReports  # TODO: invoice report download
GetIssuedInvoiceReportsJson   # TODO: invoice report JSON
